kind = "web"
previewPath = "/"
title = "MarketPulse Terminal"
version = "1.0.0"
id = "artifacts/marketpulse"
router = "path"

[[integratedSkills]]
name = "react-vite"
version = "1.0.0"

[[services]]
name = "web"
paths = [ "/" ]
localPort = 19460

[services.development]
run = "pnpm --filter @workspace/marketpulse run dev"

[services.production]
build = [ "pnpm", "--filter", "@workspace/marketpulse", "run", "build" ]
publicDir = "artifacts/marketpulse/dist/public"
serve = "static"

[[services.production.rewrites]]
from = "/*"
to = "/index.html"

[services.env]
PORT = "19460"
BASE_PATH = "/"
